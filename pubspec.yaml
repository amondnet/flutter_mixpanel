name: flutter_mixpanel
description: Flutter Plugin for Mixpanel Native
version: 1.0.0-1.0.nullsafety.0
homepage: https://github.com/amondnet/flutter_mixpanel

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=2.0.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  json_annotation: ^4.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  json_serializable: ^4.1.2
  build_runner: ^2.0.3
# For information on the generic Dart part of this file, see the
# following page: https://www.dartlang.org/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The androidPackage and pluginClass identifiers should not ordinarily
  # be modified. They are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      android:
        package: net.amond.flutter_mixpanel
        pluginClass: FlutterMixpanelPlugin
      ios:
        pluginClass: FlutterMixpanelPlugin
#builders:
#  json_serializable: ^3.2.5

